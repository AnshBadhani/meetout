# meetout
The name of this application can  change on the basis of  availability  at  time of launch but the purpose will be same till the end .


Use following Command If issue in Building Project

npm install -g typescript ts-node
npm install --save @types/express express body-parser mongoose   nodemon
